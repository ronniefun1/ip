package reboot.command;

/**
 * Represents the possible commands that can be taken in by Reboot.
 */
public enum Commands {
    LIST, MARK, UNMARK, TODO, DEADLINE,
    EVENT, DELETE, BYE, CLEAR, FIND
}
